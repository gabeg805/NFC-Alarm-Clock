<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout
	xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:app="http://schemas.android.com/apk/res-auto"
	android:id="@+id/activity_main"
	android:layout_width="match_parent"
	android:layout_height="match_parent"
	app:layout_behavior="@string/appbar_scrolling_view_behavior">

	<android.support.v7.widget.RecyclerView
		android:id="@+id/content_alarm_list"
		android:layout_width="match_parent"
		android:layout_height="match_parent"
		android:layout_marginBottom="@dimen/mb_fab"
		android:paddingBottom="@dimen/sz_fab"
		android:clipToPadding="false"
		android:scrollbars="vertical"
		android:layoutAnimation="@anim/layout_animation_fall_down"
		android:descendantFocusability="blocksDescendants"
		/>

	<android.support.design.widget.FloatingActionButton
		android:id="@+id/fab_add_alarm"
		android:layout_width="@dimen/isz_fab"
		android:layout_height="@dimen/isz_fab"
		android:layout_gravity="bottom|center"
		android:layout_marginBottom="@dimen/mb_fab"
		android:src="@mipmap/plus"
		/>

</android.support.design.widget.CoordinatorLayout>
