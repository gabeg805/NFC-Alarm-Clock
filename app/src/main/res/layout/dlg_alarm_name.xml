<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
	xmlns:android="http://schemas.android.com/apk/res/android"
	android:layout_width="fill_parent"
	android:layout_height="wrap_content"
	android:orientation="vertical"
	android:paddingTop="@dimen/spt_dlg"
	android:paddingBottom="@dimen/spb_dlg"
	android:paddingStart="?dialogPreferredPadding"
	android:paddingEnd="?dialogPreferredPadding">

	<EditText
		android:id="@+id/alarm_name"
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		android:layout_marginTop="@dimen/m_dlg_name"
		android:layout_marginBottom="@dimen/m_dlg_name"
		android:maxLength="@integer/max_entry_length"
		android:maxLines="10"
		android:singleLine="false"
		android:isScrollContainer="true"
		android:textSize="@dimen/tsz_dlg_name"
		android:inputType="textCapSentences|textMultiLine"
		android:hint="@string/dlg_name_hint">

		<requestFocus />

	</EditText>

</LinearLayout>
