<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <android.support.v7.widget.CardView
        android:id="@+id/card_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:layout_margin="@dimen/card_margin"
        android:elevation="3dp"
        card_view:cardBackgroundColor="@color/maroon"
        card_view:cardCornerRadius="@dimen/card_album_radius">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <ImageView
                android:id="@+id/toggleAlarm"
                android:layout_width="@dimen/toggle_alarm_size"
                android:layout_height="@dimen/toggle_alarm_size"
                android:layout_alignParentLeft="true"
                android:scaleType="centerInside"
                android:src="@mipmap/ic_alarm_white_24dp"
                />

            <TextView
                android:id="@+id/alarmTime"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_toRightOf="@id/toggleAlarm"
                android:paddingTop="@dimen/alarm_top_padding"
                android:paddingBottom="@dimen/alarm_time_spacing"
                android:paddingLeft="@dimen/alarm_toggle_spacing"
                android:textColor="@color/album_title"
                android:textSize="@dimen/album_title"
                />

            <TextView
                android:id="@+id/alarmInfo"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_toRightOf="@id/toggleAlarm"
                android:layout_below="@id/alarmTime"
                android:paddingBottom="@dimen/alarm_bottom_padding"
                android:paddingLeft="@dimen/alarm_toggle_spacing"
                android:textSize="@dimen/songs_count"
                />

            <ImageView
                android:id="@+id/overflow"
                android:layout_width="@dimen/ic_album_overflow_width"
                android:layout_height="@dimen/ic_album_overflow_height"
                android:layout_alignParentRight="true"
                android:scaleType="centerInside"
                android:src="@mipmap/ic_more_vert_white_24dp"
                />
        </RelativeLayout>

    </android.support.v7.widget.CardView>

</LinearLayout>
